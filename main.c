#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <assert.h>
#include "DbmsPro.h"

//To create a new student

void main()
{
    init();
    joinL();
    printf(hdr.nextL->Name);
    leave("19ucs219");
    if(hdr.nextL == NULL)
        printf("deleted");
}